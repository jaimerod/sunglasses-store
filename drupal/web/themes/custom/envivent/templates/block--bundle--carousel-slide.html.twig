{#
/**
 * @file
 * Default theme implementation to display a block.
 *
 * Available variables:
 * - plugin_id: The ID of the block implementation.
 * - label: The configured label of the block if visible.
 * - configuration: A list of the block's configuration values.
 *   - label: The configured label for the block.
 *   - label_display: The display settings for the label.
 *   - provider: The module or other provider that provided this block plugin.
 *   - Block plugin specific settings will also be stored here.
 * - content: The content of this block.
 * - attributes: array of HTML attributes populated by modules, intended to
 *   be added to the main container tag of this template.
 *   - id: A valid HTML ID and guaranteed unique.
 * - title_attributes: Same as attributes, except applied to the main title
 *   tag that appears in the template.
 * - title_prefix: Additional output populated by modules, intended to be
 *   displayed in front of the main title tag that appears in the template.
 * - title_suffix: Additional output populated by modules, intended to be
 *   displayed after the main title tag that appears in the template.
 *
 * @see template_preprocess_block()
 *
 * @ingroup themeable
 */
#}

{# debugging is easy #}
{#{ dd() }#}

{# Responsive values for all custom blocks #}
{% set uuid               = content['#block_content'].uuid.value %}
{% set css_classes        = content["#block_content"].field_css_classes.value %}
{% set css_id             = content["#block_content"].field_css_id.value|length ? content["#block_content"].field_css_id.value : 'uuid-' ~ uuid %}
{% set css_style_block    = content["#block_content"].field_css_style_block.value %}
{% set hide_on_mobile     = content["#block_content"].field_hide_on_mobile.value  ? "mobile-hide"  : "" %}
{% set hide_on_tablet     = content["#block_content"].field_hide_on_tablet.value  ? "tablet-hide"  : "" %}
{% set hide_on_desktop    = content["#block_content"].field_hide_on_desktop.value ? "desktop-hide" : "" %}

{# Responsive values for this custom block type #}
{% set full_width         = content['#block_content'].field_full_width.value ? 'full-width' : '' %}
{% set desktop_background = file_url(content['#block_content'].field_default_background_image|file_uri)|length > 1 ? file_url(content['#block_content'].field_default_background_image|file_uri) : "" %}
{% set mobile_background  = file_url(content['#block_content'].field_mobile_background_image|file_uri)|length > 1 ? file_url(content['#block_content'].field_mobile_background_image|file_uri) : "" %}

{%
  set classes = [
    'block',
    'block--type--carousel-slide',
    'block--' ~ configuration.provider|clean_class,
    'block--' ~ plugin_id|clean_class,
    css_classes,
    hide_on_mobile,
    hide_on_tablet,
    hide_on_desktop,
    full_width
  ]
%}

<div{{attributes.addClass(classes).setAttribute('id', css_id)}}>
  {{ title_prefix }}
  <style>
    {% if css_style_block %}
      {{ css_style_block }}
    {% endif %}

    {% if mobile_background|length %}
      #{{ attributes.id }} { background-image: url('{{ mobile_background }}'); }

      @media (min-width: 768px) {
    {% endif %}

      {% if desktop_background %}
        #{{ attributes.id }} {
          background-image: url('{{ desktop_background }}');
        }
      {% endif %}

     {% if mobile_background %} } {%  endif %}
  </style>

  {% if label %}
    <h2{{ title_attributes }}>{{ label }}</h2>
  {% endif %}
  {{ title_suffix }}
  {% block content %}
    {{ content }}
  {% endblock %}
</div>
